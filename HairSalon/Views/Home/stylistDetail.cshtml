<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Hair Salon</title>
  <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
  <script src="~/js/jquery-3.2.1.min.js"></script>
  <link rel='stylesheet' href="~/css/styles.css">
</head>
<body>
  <div class = "container">
    <div class = "row">
      <div class = "col-md-12">
        <h2> Stylist Information Page </h2>
        @if (Model != null)
        {
          <h4>Name:@Model.GetName()</h4>
          <h4>Clients: @Model.FindClients()</h4>
          <button type="button" class="btn"><a href="/">Home</a></button>
          <button type="button" class="btn"><a href="/new/client/@Model.GetId()">Schedule an Appointment</a></button>
          <button type="button" class="btn"><a href="/stylists">All Stylists</a></button>
        }
      </div>
    </div>
  <div id="footer"><br> Alexander Neumann February 23rd, 2018</div>
</body>
