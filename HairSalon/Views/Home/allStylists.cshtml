<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Hair Salon</title>
  <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
  <script src="~/js/jquery-3.2.1.min.js"></script>
  <link rel='stylesheet' href="~/css/styles.css">
</head>
<body>
  <div class = "container">
    <div class = "jumbotron">
      <h1>Simple Hair Salon Site</h1>
    </div>
    <div class = "row">
      <div class="col-md-12">
        <button type="button" class="btn"><a href="/">Home</a></button>
        <button type="button" class="btn"><a href="/new/stylist">Register a new Stylist</a></button>
        <h2> All Stylists </h2>
        @if (Model != null)
        {
            @foreach(var item in Model)
            {
              <h3>Stylist<a href='/stylists/@item.GetId()'> @item.GetId() </a></h3>
              <h4>Stylist Name:@item.GetName()</h4>
            }
        }
      </div>
    </div>
  </div>
  <div id="footer"><br> Alexander Neumann February 23rd, 2018</div>
</body>
